import Navigation from "../Navigation/Navigation";
import "./Header.css"
const Header = () => {
    return (
        <header>
            <Navigation />
        </header>
    );
}

export default Header;